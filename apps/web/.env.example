# Web (Next.js) public env — loaded automatically from this app directory.
# NEXT_PUBLIC_* values reach the browser, so only non-secret publishable values belong here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=http://localhost:4000
